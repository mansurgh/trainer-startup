import 'package:flutter/material.dart';

class T {
  static const bg = Color(0xFF000000);         // Pure black
  static const card = Color(0xFF1A1A1A);       // Dark gray
  static const cardElevated = Color(0xFF2A2A2A); // Light gray
  static const text = Colors.white;            // White text
  static const textSec = Color(0xFF9AA0A6);    // Gray text
  static const accent = Colors.white;          // White accent (not green!)
  static const accentMuted = Color(0xFF2A2A2A); // Dark gray
  static const border = Color(0xFF2A2A2A);     // Gray border
  static const success = Color(0xFF34C759);    // Keep green for success
  static const muscle = Color(0xFFFF3B30);     // Keep red for muscle

  static const r16 = Radius.circular(16);
  static const r12 = Radius.circular(12);
  static const r10 = Radius.circular(10);

  static const p16 = EdgeInsets.all(16);
  static const p12 = EdgeInsets.all(12);
}